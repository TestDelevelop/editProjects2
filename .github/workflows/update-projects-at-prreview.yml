name: Update Projects at Pull Request Review

on:
  pull_request_review:
    types: 
      - 'edited'
      - 'dismissed'
      - 'submitted'
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - id: 'reviews'
        uses: 'jrylan/github-action-reviews-counter@main'
        with:
          repo-token: '${{ secrets.GITHUB_TOKEN }}'
        run: |
          test=steps.reviews.outputs.approved
          echo $test